package com.gmail.tatsukimatsumo.kotlintodo.tasks

import android.os.Bundle
import com.gmail.tatsukimatsumo.kotlintodo.data.Task
import org.junit.Test

import org.junit.Assert.*
import org.junit.Before
import org.mockito.ArgumentCaptor
import org.mockito.Mock
import org.mockito.Mockito.*
import org.mockito.MockitoAnnotations

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */
class TasksPresenterTest {
//    @Mock private lateinit var tasksView: TasksContract.View
//    private lateinit var tasksPresenter: TasksPresenter
//    private lateinit var tasks: Array<Task>
//
//    @Before
//    fun setup() {
//        MockitoAnnotations.initMocks(this)
//
//        tasksPresenter = TasksPresenter(tasksView)
//        tasks = arrayOf(
//            Task("Title1", "Description1"),
//            Task("Title2", "Description2"),
//            Task("Title3", "Description3")
//        )
//    }
//
    @Test
    fun testOnViewCreated() {
////        val updateArgumentCapture = ArgumentCaptor.forClass(Array<Task>::class.java)
////        verify(tasksView).update(updateArgumentCapture.capture())
////
////        tasksPresenter.onViewCreated()
////
////
////
////
////
////        assertTrue(updateArgumentCapture.value.size == 3)
    }
}
